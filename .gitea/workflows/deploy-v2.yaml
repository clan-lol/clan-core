name: deploy-v2
on:
  push:
    branches:
      - main
jobs:
  deploy-docs:
    runs-on: nix
    steps:
      - uses: actions/checkout@v4
      - run: nix run --print-build-logs .#deploy-docs-v2 -- --current
    env:
      SSH_HOMEPAGE_KEY: ${{ secrets.SSH_HOMEPAGE_KEY }}
